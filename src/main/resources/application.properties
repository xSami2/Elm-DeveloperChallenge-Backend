# Server Configuration
spring.application.name=developerChallenge
server.port=9090




## PostgresSQL Database Configuration
#spring.datasource.url=jdbc:postgresql://localhost:5432/project-x
#spring.datasource.username=postgres
#spring.datasource.password=admin
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=none
#spring.datasource.initialization-mode=always
#spring.h2.console.enabled=false
#
#
# Liquibase configuration
#spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml
#spring.liquibase.enabled=true

##################################

## H2

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.datasource.platform=h2

# Hibernate settings for JPA
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# DDL mode (create/drop schema automatically at startup)
spring.jpa.hibernate.ddl-auto=create-drop

# Enable console for H2 database (optional)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console  # Access it through /h2-console path in the browser
